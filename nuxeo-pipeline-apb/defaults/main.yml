---

# Defaults for values we could expect from a broker

_apb_plan_id: default
_apb_service_class_id: 0
_apb_service_instance_id: 0

default_debug: True
bootstrap_debug_enabled: False

apb_name: nuxeo-pipeline-apb
app_image: docker.io/nuxeoapbcatalog/nuxeo-pipeline-apb
namespace: "{{ lookup('env','NAMESPACE') | default('int-apb-dev', true) }}"
component: pipeline

project_name: nuxeo
pipeline_name: default
pipeline_git_ref: master
pipeline_definition: test-dev

